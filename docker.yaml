FROM maven:3.9.9-jdk-17 AS build COPY.. RUN mvn clean package

FROM openjdk:17-jdk-slim COPY --from-build
/target/school-management-0.0.1-SNAPSHOT.jar school.jar

EXPOSE 8080 ENTRYPOINT ["java", "-jar", "school.jar"]